name: "🧪 주간 빌드 테스트 체크리스트"
description: 매주 빌드 결과를 점검하고 기록합니다.
title: "chore: 주간 빌드 테스트 - [날짜/주차]"
labels:
  - QA
  - weekly-build
assignees: []

body:
  - type: markdown
    attributes:
      value: |
        ## 🧪 주간 빌드 테스트
        
        이번 주 빌드가 정상적으로 동작하는지 확인하고, 발견된 이슈를 기록해 주세요.

  - type: input
    id: build_info
    attributes:
      label: 빌드 정보
      description: 빌드 번호, 브랜치, 커밋 해시 등을 적어주세요.
      placeholder: 예) Android v0.1.3 (branch develop, commit a1b2c3d)
    validations:
      required: true

  - type: input
    id: test_date
    attributes:
      label: 테스트 일시
      description: 테스트를 진행한 날짜/시간을 적어주세요.
      placeholder: 예) 2025-11-14 22:30
    validations:
      required: true

  - type: checkboxes
    id: basic_check
    attributes:
      label: 기본 동작 확인
      description: 기본적으로 항상 확인해야 하는 항목들입니다.
      options:
        - label: 앱이 정상적으로 실행된다.
          required: true
        - label: 게임 화면까지 정상적으로 진입 가능하다.
          required: true
        - label: 기본 플레이(이동, 공격/상호작용 등)가 1판 이상 정상적으로 진행된다.
          required: true

  - type: checkboxes
    id: feature_check
    attributes:
      label: 이번 주 주요 점검 항목
      description: 이번 주에 변경되었거나 집중해서 봐야 하는 기능들을 체크해 주세요.
      options:
        - label: 이번 주에 추가/수정된 기능이 의도대로 동작한다.
          required: false
        - label: 기존 핵심 기능(이동, 전투, 업그레이드, 스테이지 진행 등)에 치명적인 리그레션이 없다.
          required: false
        - label: UI 배치/텍스트 깨짐, 해상도 관련 문제 없음.
          required: false
        - label: 성능(프레임, 로딩 속도 등)이 눈에 띄게 저하되지 않았다.
          required: false

  - type: dropdown
    id: result
    attributes:
      label: 전체 결과
      description: 이번 주 빌드 테스트의 전체적인 결과를 선택해 주세요.
      options:
        - ✅ 통과 (크리티컬 이슈 없음)
        - ⚠️ 조건부 통과 (치명적이지 않은 이슈 있음)
        - ❌ 실패 (크리티컬 이슈로 배포 불가)
    validations:
      required: true

  - type: textarea
    id: summary
    attributes:
      label: 테스트 요약
      description: 전체적인 빌드 상태를 간단히 정리해 주세요.
      placeholder: 예) 전반적으로 플레이 가능 상태이나, 3스테이지 보스 패턴 버그로 인해 조건부 통과 처리.
    validations:
      required: true

  - type: textarea
    id: bugs
    attributes:
      label: 발견된 이슈/버그
      description: 발견한 버그를 이슈 번호와 함께 적어주세요. (가능하면 별도 Bug 이슈를 만들고 링크해 주세요.)
      placeholder: |
        - #123: 스테이지 2-3에서 포탈이 생성되지 않는 문제
        - #124: 업그레이드 UI에서 텍스트가 잘리는 현상
    validations:
      required: false

  - type: textarea
    id: repro_steps
    attributes:
      label: 재현이 필요한 이슈 상세 (선택)
      description: 재현이 까다롭거나 기록이 필요한 이슈는 단계별로 정리해 주세요.
      placeholder: |
        1) 타이틀 화면에서 'Continue' 선택
        2) Planet-2 진입 후 3웨이브까지 진행
        3) 보스 등장 직후 스킬 사용 시 앱이 강제 종료됨
    validations:
      required: false

  - type: textarea
    id: etc
    attributes:
      label: 기타 메모
      description: 스크린샷 경로, 동영상 링크, 다음 주에 집중해야 할 포인트 등을 적어주세요.
      placeholder: 예) 스크린샷 /Capture/2025-11-14/, 다음 주에 2-3 스테이지 밸런스 재점검 필요.
    validations:
      required: false
